
<html>
  <head>
    <title>Flapper News</title>
    <link href="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">

    <script src = "http://ajax.googleapis.com/ajax/libs/angularjs/1.3.10/angular.min.js"></script>
    <script src = "/javascripts/angularApp.js"></script>
    <style> .glyphicon-thumbs-up { cursor:pointer } </style>
  </head>
  <body ng-app = "flapperNews" ng-controller = "MainCtrl">
    <div class = "row">
      <div class = "col-md-6 col-md-offset-3">
        <div class = "page-header">
          <h1>Flapper News</h1>
        </div>
        <div ng-repeat = "post in posts | orderBy: '-upvotes'">
          <span class = "glyphicon glyphicon-thumbs-up" ng-click = "incrementUpvotes(post)"></span>
          <a ng-show = "post.link" href = "{{post.link}}">
            {{post.title}}
          </a>
          <span ng-hide = "post.link">
            {{post.title}}
          </span>
           - upvotes {{post.upvotes}}
        </div>
        <h3>Add post:</h3>
        <form style = "margin-top:30px;" ng-submit = "addPost()">
          <h3>Add a new post</h3>
          <input type = "text"
                class = "form-control"
                placeholder = "title"
                ng-model = "title" />
          <input type = "text"
                class = "form-control"
                placeholder = "link"
                ng-model = "link" />
          <button type = "Submit" class = "btn btn-primary">Post</button> <!-- ng-click directive used to select function from app.js. If ng-click is left on sumbit in form it will run the method multiple times-->
        </form>
      </div>
    </div>
  </body>
</html>
